

public class Questions_Arrays {
    public static void main(String[] args) {

        //Q1. Take an array of names as input from the user and print them on the screen.

        // Scanner sc= new Scanner(System.in);
        // System.err.print("Enter the size of array:- ");
        // int size =sc.nextInt();
        // String name[] =new String[size];
        // System.err.println("Enter the names:- ");
        // for (int i = 0; i < name.length; i++) {
        //     name[i]=sc.next();
        // }
        // System.err.println("--------------------Display the Array-------------");
        // //Output
        // for(int i=0 ;i< name.length;i++){
        //     System.err.println(name[i]);
        // }


        // Q2. Find the maximum & minimum number in an array of integers. 
        // Scanner sc = new Scanner(System.in);
        // System.out.println("Enter the size of Array:- ");
        // int size = sc.nextInt();
        // System.err.println("Enter the elements of the array:- ");
        // int numbers[] =new int[size];
        // //Input
        // for (int i = 0; i < numbers.length ; i++) {
        //     numbers[i]= sc.nextInt();
        // }
        // //OutPut
        // int maximum=0;
        // int minimum=numbers[0];
        // for (int i = 0; i < numbers.length; i++) {
        //     if(numbers[i] > maximum){
        //         maximum = numbers[i];
        //     }
        //     if(numbers[i] <minimum){
        //         minimum =numbers[i];
        //     }  
        // }
        // System.err.println("Largest number is: "+maximum);
        // System.err.println("Smallest number is: "+minimum);


        //// Q3. Take an array of numbers as input and check if it is an array sorted in ascending order.
        ////     Eg : { 1, 2, 4, 7 } is sorted in ascending order.
        ////     {3, 4, 6, 2} is not sorted in ascending order.
        // Scanner sc =new Scanner(System.in);
        // System.err.println("Enter the size arrays:- ");
        // int size=sc.nextInt();
        // int number[] =new int[size];
        // System.err.println("Enter the elements of array:- ");
        // //Input
        // for (int i = 0; i < number.length; i++) {
        //     number[i] =sc.nextInt();
        // }
        // boolean Ascending =true;

        // for (int i = 0; i < number.length-1; i++) {
        //     if(number[i] > number[i+1]){
        //         Ascending =false;
        //     }
            
        // }

        // if(Ascending){
        //     System.err.println("The Given array is Asending Array");
        // }
        // else{
        //     System.err.println("The Given Array is not Asendinf Array");
        // }

    }
    
}
